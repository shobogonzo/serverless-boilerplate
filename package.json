{
  "name": "shobo-backend",
  "version": "1.0.0",
  "description": "Serverless backend boilerplate",
  "author": "Patrick Palmer",
  "license": "AGPL-3.0-only",
  "scripts": {
    "sls": "sls",
    "export-env": "sls export-env -s $STAGE && sls manifest -s $STAGE"
  },
  "dependencies": {
    "chance": "^1.1.11"
  },
  "devDependencies": {
    "@aws-sdk/client-dynamodb": "^3.518.0",
    "@aws-sdk/lib-dynamodb": "^3.518.0",
    "dotenv": "^16.4.5",
    "dotenv-cli": "^7.3.0",
    "serverless": "^3.38.0",
    "serverless-appsync-plugin": "^2.7.0",
    "serverless-export-env": "^2.2.0",
    "serverless-manifest-plugin": "^1.0.9"
  }
}
